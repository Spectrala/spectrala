/**
 * These are colors referenced by interface elements.
 * */

import colors from './colors';

const theme = {

    // Camera line
    LOW_ENERGY_THUMBNAIL: colors.LOW_ENERGY,
    HIGH_ENERGY_THUMBNAIL: colors.HIGH_ENERGY,
    CAMERA_LINE_COLOR: colors.WHITE,

    // Cards
    CARD_HEADER: colors.FOREGROUND_1,
    CARD_BODY: colors.FOREGROUND_2,
    CARD_OUTLINE: colors.NONE,

};

export default theme;
