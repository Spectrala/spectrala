/**
 * These are colors referenced by interface elements.
 * */

import colors from './colors';

const theme = {

    // General 
    CONTAINER_BACKGROUND: colors.BACKGROUND,
    NAV: colors.BRAND,
    SHADOW_COLOR: colors.GRAY,
    BORDER_RADIUS: 2,
    BORDER_WIDTH: 0,
    NAV_HEIGHT: "80px",


    VERTICAL_BAR_HEIGHT: '64px',


    // Camera line
    LOW_ENERGY_THUMBNAIL: colors.LOW_ENERGY,
    HIGH_ENERGY_THUMBNAIL: colors.HIGH_ENERGY,
    CAMERA_LINE_COLOR: colors.WHITE,

    // Cards
    CARD_HEADER: colors.WHITE,
    CARD_HEADER_PADDING: '0px',
    CARD_BODY: colors.FOREGROUND_1,
    CARD_OUTLINE: colors.NONE,

};

export default theme;
