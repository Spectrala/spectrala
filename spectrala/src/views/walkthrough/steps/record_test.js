import React from 'react';

export default (
    <p>
        The "Spectrum" chart is still a live view of the spectrum inside the
        spectrometer like in the last section. With a reference spectrum
        recorded, live transmittance and absorbance values can be viewed by
        selecting them above the "Spectrum" chart.
    </p>
);
